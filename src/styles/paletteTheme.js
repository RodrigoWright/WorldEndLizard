import { createTheme } from '@mui/material/styles';

export const palette = createTheme({
  palette: {
    mode: 'dark',
    primary: {
      main: '#ff5722',
      light: '#f6ff5b',
      dark: '#b71c1c',
    },
    secondary: {
      main: '#42a9da',
    },
    
  },
});